function init() {
/*  const headers = document.querySelectorAll(".article-header-title")

  const ddd = new IntersectionObserver((entries) => {
    console.log(entries)
  })
  
  headers.forEach(v => ddd.observe(v))*/
}

init()
